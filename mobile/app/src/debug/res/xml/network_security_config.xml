<network-security-config>
    <!-- Debug-only: auth-service runs over plain HTTP in local dev (see backend/README.md's
         Docker section). 10.0.2.2 is the Android emulator's alias for the host machine.
         Deliberately NOT in src/main, so a release build never permits cleartext traffic. -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
